# Production profile (activated with SPRING_PROFILES_ACTIVE=prod).
# Inherits everything from application.properties and overrides only what differs.
spring.jpa.hibernate.ddl-auto=update
# do not run data.sql demo seed (fake usage stats) against the persistent prod database
spring.sql.init.mode=never
