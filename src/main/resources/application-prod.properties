# Deliberately redundant with the base configuration: these are already the
# defaults, repeated here so that reading this file tells you what production
# does without having to check what it inherits.
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=never
